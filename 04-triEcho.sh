#!/bin/bash

word="awesome"

# Using variables allows us the flexibility to reduce the repetitive behaviour.
# We cannot change the script every single time hence we use variables to modify
# the parts of the program that are likely to change. 

echo "Linux is $word"
echo "Videogames are $word"
